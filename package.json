{
  "name": "student-results-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "build": "esbuild worker-backend.js --bundle --outfile=dist/worker.js --format=esm --platform=node --target=es2022"
  },
  "dependencies": {
    "bcrypt-ts": "^5.0.2",
    "drizzle-orm": "^0.31.2",
    "hono": "^4.4.12",
    "jose": "^5.6.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "wrangler": "^3.60.3",
    "drizzle-kit": "^0.22.7",
    "esbuild": "^0.21.5"
  }
}